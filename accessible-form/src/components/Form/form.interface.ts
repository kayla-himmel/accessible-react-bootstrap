// import react from 'react';
// import { BootstrapFormProps } from 'react-bootstrap/Form'

interface FormProps extends BootstrapFormProps {
    label: string,
    value: string,
};

export default FormProps;
